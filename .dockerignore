# Dependencies and build output (reinstalled/copied in image)
node_modules
build
.git
.gitignore

# Env and secrets - never bake into image; Railway injects at runtime
.env
.env.*
!.env.example

# Large or sensitive local data (seed data is copied in Dockerfile from builder)
# data/

# IDE and OS
.cursor
.vscode
.DS_Store
*.log
npm-debug.log*

# Docs and non-runtime
docs
HubInstall
*.md
!package.json
php
images
js
css
fonts
