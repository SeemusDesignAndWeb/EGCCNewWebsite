# Hub CRM Configuration

# Required: 32-byte base64-encoded key for encryption
# Generate with: openssl rand -base64 32
CRM_SECRET_KEY=

# Optional: Data directory path (defaults to ./data)
CRM_DATA_DIR=./data

# Required for email sending via Resend
RESEND_API_KEY=

# Optional: Base URL for emails (defaults to request origin)
APP_BASE_URL=http://localhost:5173

# Required for rota reminder cron job security
# Generate with: openssl rand -base64 32
ROTA_REMINDER_CRON_SECRET=

# Optional: Days ahead for rota reminders (defaults to 3)
ROTA_REMINDER_DAYS_AHEAD=3
